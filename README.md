# html-works

统一存放可通过 GitHub Pages 长期访问的 HTML 网页作品。

## 目录约定

每个作品固定放在：

```text
docs/<英文短名>/index.html
```

总目录为 `docs/index.html`。GitHub Pages 使用 `main` 分支的 `/docs` 目录发布。

## 当前作品

- `reflection-target`：反射打靶
- `fraction-cut-king`：分数切割王
- `ratio-color-master`：比例调色师
